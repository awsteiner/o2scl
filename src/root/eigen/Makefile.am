
if O2SCL_EIGEN
if O2SCL_ARMA
HEADER_VAR = 
else
HEADER_VAR = mroot_special.h
endif
else
HEADER_VAR = 
endif

# ------------------------------------------------------------
# Install headers
# ------------------------------------------------------------

am__pkginclude_HEADERS_DIST = mroot_special.h

pkginclude_HEADERS = $(HEADER_VAR)


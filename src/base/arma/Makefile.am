
if O2SCL_ARMA
if O2SCL_EIGEN
HEADER_VAR = 
else
HEADER_VAR = vector_special.h
endif
else
HEADER_VAR = 
endif

# ------------------------------------------------------------
# Install headers
# ------------------------------------------------------------

am__pkginclude_HEADERS_DIST = vector_special.h

pkginclude_HEADERS = $(HEADER_VAR)


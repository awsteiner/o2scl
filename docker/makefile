help:
	@echo "Help."

build_opensuse_leap:
	sudo docker build - < opensuse_leap

build_ubuntu:
	sudo docker build - < ubuntu > ubuntu.out 2>&1 &

build_ubuntu_dev:
	sudo docker build - < ubuntu_dev > ubuntu_dev.out 2>&1 &
